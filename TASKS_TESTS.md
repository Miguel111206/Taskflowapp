# Tests añadidos

Asignado: Copilot
- Backend: escribir tests pytest para endpoints CRUD y cambios de estado.
  - Archivo: backend/tests/test_api.py
  - Usa una base de datos SQLite en memoria para pruebas.

Asignado: Codex
- Frontend: (opcional) escribir tests de integración UI (vitest/jest) y ajustar api.js si es necesario.

Notas:
- Instalar dependencias: pip install -r backend\requirements.txt
- Ejecutar tests: cd backend && pytest -q
